<?xml version="1.0" encoding="UTF-8"?>
<!-- QuantumLink IM 统一日志配置(时间戳 + 日期滚动)
     使用: java -Dlogback.configurationFile=scripts/logback.xml -jar im-connect/target/im-connect-1.0.0-SNAPSHOT.jar
     或放入 im-connect/src/main/resources/ 重新打包(classpath 自动生效)。
     RCAgent 的时间截止约束依赖行首 ISO 时间戳。 -->
<configuration>
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level --- [%thread] %logger{40} : %msg%n</pattern>
    </encoder>
  </appender>
  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>logs/connect.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>logs/connect.%d{yyyyMMdd}.log</fileNamePattern>
      <maxHistory>7</maxHistory>
    </rollingPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level --- [%thread] %logger{40} : %msg%n</pattern>
    </encoder>
  </appender>
  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="FILE"/>
  </root>
</configuration>
